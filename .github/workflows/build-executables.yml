name: Multi-Platform Binary Compilation (.exe, .dmg, .AppImage, .apk)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-desktop:
    name: Build Desktop Executables (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
          - os: macos-latest
            platform: macos
          - os: ubuntu-latest
            platform: linux

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Linux Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y fuse libfuse2 libfuse2t64 desktop-file-utils libgtk-3-dev || sudo apt-get install -y fuse libfuse2 desktop-file-utils libgtk-3-dev

      - name: Install Node Dependencies
        run: npm install --legacy-peer-deps

      - name: Build Web Application
        run: npm run build

      - name: Inject Electron Configs into package.json
        shell: bash
        run: |
          node -e "
          const fs = require('fs');
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          pkg.main = 'electron-main.js';
          pkg.author = pkg.author || 'Meta-AI Builder';
          pkg.description = pkg.description || 'Meta-AI Web Builder Cross-Platform Application';
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
          "

          cat << 'EOF' > electron-main.js
          const { app, BrowserWindow } = require('electron');
          const path = require('path');

          function createWindow() {
            const win = new BrowserWindow({
              width: 1280,
              height: 800,
              webPreferences: { nodeIntegration: true, contextIsolation: false }
            });
            win.loadFile(path.join(__dirname, 'dist/index.html'));
          }

          app.whenReady().then(createWindow);
          app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit(); });
          EOF

          cat << 'EOF' > electron-builder.json
          {
            "appId": "com.metaai.webbuilder",
            "productName": "Meta-AI-Builder",
            "directories": { "output": "release-builds" },
            "files": ["dist/**/*", "electron-main.js", "package.json"],
            "win": { "target": ["zip", "portable"] },
            "mac": {
              "target": ["dmg", "zip"],
              "identity": null,
              "hardenedRuntime": false,
              "gatekeeperAssess": false
            },
            "linux": { "target": ["AppImage"] }
          }
          EOF

      - name: Compile Desktop Executables
        shell: bash
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: |
          if [ "${{ matrix.platform }}" == "windows" ]; then
            npx electron-builder --win --config electron-builder.json
          elif [ "${{ matrix.platform }}" == "macos" ]; then
            npx electron-builder --mac --config electron-builder.json
          else
            npx electron-builder --linux --config electron-builder.json
          fi

      - name: Upload Desktop Binaries
        uses: actions/upload-artifact@v4
        with:
          name: meta-ai-builder-${{ matrix.platform }}-binary
          if-no-files-found: warn
          path: release-builds/*

  build-android:
    name: Build Android APK (.apk)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Node Dependencies
        run: npm install --legacy-peer-deps

      - name: Build Web Application
        run: npm run build

      - name: Scaffold Android Capacitor Platform
        run: |
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          npx cap copy android
          npx cap sync android

      - name: Compile Android Debug APK
        run: |
          if [ -d "android" ]; then
            cd android
            chmod +x gradlew
            ./gradlew assembleDebug --no-daemon
          fi

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: meta-ai-builder-android-apk
          if-no-files-found: warn
          path: android/app/build/outputs/apk/debug/app-debug.apk
